# Direct dependencies with lower bounds.
# For a byte-exact environment use requirements-lock.txt (generated by pip freeze).

mne>=1.6            # EDF + hypnogram annotation reading, sleep_physionet fetcher
numpy>=1.26
scipy>=1.11         # signal.welch, integrate.simpson
scikit-learn>=1.4   # HistGradientBoostingClassifier(class_weight=...) needs >=1.4
matplotlib>=3.8
pytest>=8.0
